/*  LOGIN PAGE  AND LOGOUT PAGE ROUTES*/
 /* TODO LIST
   1.Include module ('express').Router()
   2.Include "user model" (it has the user data login data verifications)
   3.Post route to the root(/) 
   4.Post root to the login page.
      *validate the user's login email data
      *validate the user's password (like length)
      *if user login data is validated
        render a welcome message with user name,
      else
       render a invalid login name or password
   5.Post route for logout page
   6.Exports this module as router */

